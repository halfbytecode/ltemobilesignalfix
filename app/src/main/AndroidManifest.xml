<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.hbc.ltemobilesignalfix"
	android:versionCode="6"
    android:versionName="1" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
	<meta-data android:value="true" android:name="xposedmodule"/>
	<meta-data android:value="2.0*" android:name="xposedminversion"/>
	<meta-data
		android:value="Makes LTE signal use RSRP for signal level measurement, ignoring RSSNR values. This is the default behavior for most OEMs. However, some custom ROMs do not include this."
		android:name="xposeddescription"/>
    </application>

</manifest>
